<!DOCTYPE html>

<title>Edit SyStates</title>

{% block body %}
    <h1>Edit SyStates</h1>

    {{ include('sy_states/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('sy_states_index') }}">back to list</a>

    {{ include('sy_states/_delete_form.html.twig') }}
{% endblock %}
