<form method="post" action="{{ path('sy_states_delete', {'id': sy_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sy_state.id) }}">
    <button class="btn">Delete</button>
</form>
