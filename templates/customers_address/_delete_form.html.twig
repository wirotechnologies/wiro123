<form method="post" action="{{ path('customers_address_delete', {'id': customers_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customers_address.id) }}">
    <button class="btn">Delete</button>
</form>
