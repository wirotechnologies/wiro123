<!DOCTYPE html>

<title>New Payments</title>

{% block body %}
    <h1>Create new Payments</h1>

    {{ include('payments/_form.html.twig') }}

    <a href="{{ path('payments_index') }}">back to list</a>
{% endblock %}
