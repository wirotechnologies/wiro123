<form method="post" action="{{ path('socioeconomic_levels_delete', {'id': socioeconomic_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socioeconomic_level.id) }}">
    <button class="btn">Delete</button>
</form>
