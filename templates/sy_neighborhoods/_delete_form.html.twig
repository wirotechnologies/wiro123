<form method="post" action="{{ path('sy_neighborhoods_delete', {'id': sy_neighborhood.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sy_neighborhood.id) }}">
    <button class="btn">Delete</button>
</form>
